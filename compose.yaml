services:
  afra-app:
    build:
      context: .
      dockerfile: Afra-App/Dockerfile
    ports:
        - 8080:8080
  afra-app-web:
    build:
        context: .
        dockerfile: ./WebClient/Dockerfile
    ports:
        - 3000:3000
