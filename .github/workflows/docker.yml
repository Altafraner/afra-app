name: Build Docker Images

on:
  push:
    branches: [ "main" ]
    paths:
      - 'Afra-App/**'
      - 'WebClient/**'
      - 'compose.yaml'
      - '.dockerignore'
  pull_request:
    branches: [ "main" ]
    paths:
      - 'Afra-App/**'
      - 'WebClient/**'
      - 'compose.yaml'
      - '.dockerignore'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: build backend image
      run: docker build Afra-App
    - name: build frontend image
      run: docker build WebClient
    - name: build compose
      run: docker compose build
